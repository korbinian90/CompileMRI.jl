name: Compile
on:
  - push
jobs:
  test:
    name: Julia 1.7 - ${{ matrix.os }} - ${{ github.event_name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-18.04
          - macOS-latest
          - windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Compile
        run: echo ${{ runner.os }}
      
